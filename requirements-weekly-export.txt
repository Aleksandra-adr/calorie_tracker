# Зависимости workflow "недельный отчёт" (workflows/weekly_export.py).
# Отдельный файл, чтобы не трогать общий requirements.txt проекта и не
# создавать конфликт при мерже с другими ветками (см. REPORT.md про
# разбивку на изолированные зоны).
#
# fpdf2 выбран вместо reportlab: чистый Python, ставится одной командой
# без системных зависимостей, минималистичный API, достаточный для
# табличного PDF-отчёта из этого workflow. CSV использует только stdlib
# (csv) и не требует зависимостей вообще.
fpdf2>=2.8
